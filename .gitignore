# generated caches + persisted identities (regenerated from ~/.claude)
data/

# os / editor noise
.DS_Store
node_modules/
*.log

# claude code local state + nested agent worktrees (e.g. sprite-lab)
.claude/
